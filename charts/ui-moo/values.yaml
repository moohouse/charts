config:
  name: ui-moo
  instance: moo
  partof: mooheadtown
  component: front
  namespace: barn
deployment:
  replicaCount: 2
container:
  image: ghcr.io/moohouse/ui-moo
  tag: '86'
  pullPolicy: IfNotPresent
  secret: moo
service:
  type: ClusterIP
  port: 9000
  targetPort: 9000
istio:
  rollout:
    route: ["http"]
  vs:
    host: ["moo.is"]
    gateway: ["gateway-moo-is"]
    stable:
      host: ui-moo-stable.barn.svc.cluster.local
      port: 9000
    canary:
      host: ui-moo-canary.barn.svc.cluster.local
      port: 9000
pdb:
  min: 1
hpa:
  min: 2
  max: 2
  cpu: 50
  ram: 50
